<section class="container-shell pt-12 md:pt-20">
  <div class="grid gap-10 lg:grid-cols-[1.1fr_0.9fr] lg:items-center">
    <div>
      <p class="mb-4 inline-flex items-center gap-2 rounded-full border border-line bg-panel/60 px-4 py-2 text-xs uppercase tracking-[0.18em] text-accent2">
        Built for teams with many repos
      </p>
      <h1 class="text-balance text-3xl font-extrabold leading-tight sm:text-4xl md:text-6xl">
        Manage every <span class="text-accent">.env</span> file from one shelf.
      </h1>
      <p class="mt-5 max-w-xl text-pretty text-base text-muted md:text-lg">
        Select any root folder, discover all environment files grouped by project,
        edit variables safely, and save changes without jumping between editors.
      </p>
      <div class="mt-8 flex flex-wrap gap-3">
        <a
          href="/env-shelf.dmg"
          class="w-full rounded-full bg-accent px-6 py-3 text-center font-semibold text-bg transition hover:brightness-110 sm:w-auto"
        >
          Download for macOS (Apple Silicon)
        </a>
        <a
          href="https://github.com/ivanglpz/env-shelf-local"
          class="w-full rounded-full border border-line bg-panel px-6 py-3 text-center font-semibold transition hover:border-accent2 sm:w-auto"
        >
          View GitHub Repository
        </a>
      </div>
      <p class="mt-4 text-sm text-muted">env-shelf is open source.</p>
    </div>

    <div class="rounded-3xl border border-line bg-panel p-4 shadow-glow sm:p-5">
      <div class="mb-4 flex items-center gap-2 text-xs text-muted">
        <span class="h-2 w-2 rounded-full bg-[#ff6464]"></span>
        <span class="h-2 w-2 rounded-full bg-[#ffc14d]"></span>
        <span class="h-2 w-2 rounded-full bg-[#47d764]"></span>
        <span class="ml-2">env-shelf dashboard</span>
      </div>
      <div class="space-y-3">
        <div class="rounded-2xl bg-bg/80 p-4">
          <p class="text-xs uppercase tracking-[0.14em] text-muted">Workspace</p>
          <p class="mt-1 break-all text-sm font-semibold">/apps/company-suite</p>
        </div>
        <div class="grid gap-3 sm:grid-cols-2">
          <article class="rounded-2xl border border-line bg-bg/70 p-4">
            <p class="break-all text-xs text-muted">auth-service/.env</p>
            <p class="mt-2 font-semibold">12 variables</p>
          </article>
          <article class="rounded-2xl border border-line bg-bg/70 p-4">
            <p class="break-all text-xs text-muted">billing/.env.production</p>
            <p class="mt-2 font-semibold">18 variables</p>
          </article>
        </div>
        <article class="rounded-2xl border border-line bg-bg/70 p-4">
          <p class="text-xs text-muted">Editing KEY: API_ENDPOINT</p>
          <p class="mt-2 break-all text-sm">https://api.env-shelf.dev/v1</p>
        </article>
      </div>
    </div>
  </div>
</section>
