---
const features = [
  {
    title: "Folder-level discovery",
    description: "Pick one root directory and env-shelf maps every .env file by project folder automatically."
  },
  {
    title: "Safe variable editing",
    description: "Update keys and values with a clean editor, then write changes back to each file."
  },
  {
    title: "Create and delete variables",
    description: "Add missing values or remove stale keys to keep environments tidy and consistent."
  },
  {
    title: "Multilingual interface",
    description: "Built-in i18n support: English, Spanish, Portuguese, Japanese, and German."
  }
];
---

<section id="features" class="container-shell pt-14 sm:pt-16">
  <div class="mb-8 flex items-end justify-between gap-4">
    <h2 class="text-3xl font-bold md:text-4xl">Core capabilities</h2>
    <p class="hidden text-sm text-muted md:block">Designed for real-world env workflows</p>
  </div>

  <div class="grid gap-4 md:grid-cols-2">
    {
      features.map((feature, index) => (
        <article class="rounded-3xl border border-line bg-panel p-5 transition hover:-translate-y-0.5 hover:border-accent2 sm:p-6">
          <p class="mb-4 text-xs uppercase tracking-[0.16em] text-accent2">0{index + 1}</p>
          <h3 class="text-xl font-semibold">{feature.title}</h3>
          <p class="mt-3 text-muted">{feature.description}</p>
        </article>
      ))
    }
  </div>
</section>
